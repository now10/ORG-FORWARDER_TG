services:
  - type: worker
    name: signal-forwarder
    env: python
    buildCommand: pip install -r requirements.txt  ✓
    startCommand: python app.py  ✓
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    autoDeploy: true
